# 未来改进方向

记录项目中已识别但尚未实施的优化和改进项。按优先级排列，每项包含触发条件、具体方案和预期效果。

---

## 已完成功能（2026年6月）

### ✅ 1. 租客门户重构

**完成时间：** 2026-06-02

**改动内容：**
- TenantPortal 组件独立渲染，个人资料和维修反馈中心不再依赖租约状态
- 修复TAB切换问题（个人信息、维修与反馈中心现在可以正常切换）

---

### ✅ 2. 历史租约收租核查表

**完成时间：** 2026-06-02

**改动内容：**
- 历史租约卡片可展开查看付款记录
- 付款网格与中介端完全一致（6列自适应）
- 根据合约期限自动生成所有月份（不再依赖数据库中的付款记录数量）
- 显示租约终止原因（自行终止/自然到期/已归档）
- 移除历史租约TAB上的数字徽章

---

### ✅ 3. 租客删除历史租约

**完成时间：** 2026-06-02

**改动内容：**
- 历史租约卡片添加删除按钮
- 点击删除弹出确认弹窗
- 删除时同时移除付款记录（payment_records）和租约记录（leases）

---

### ✅ 4. 注销账号数据清理与 7 天取证留存

**完成时间：** 2026-06-06

**改动内容：**
- **中介自助注销**（顶栏）：有活跃租约则不允许注销；无活跃租约时立即吊销登录，REN/公司资料保留 7 天（`DELETED:时间戳` 标记），期满自动永久删除；历史租约与财务记录始终保留。
- **超管移除中介**（管理后台）：立即彻底删除（不走 7 天留存）；同样有活跃租约时拒绝。
- **租客注销 7 天留存期**：
  * **Day 0**：即时删除与 `auth.users` 相关的通知、合租意向和报修工单数据，并执行 `auth.users` 物理销户，确保租客立即失去账户登录权限。`public.users` 行保持原样（仅更新 `avatar_url = 'DELETED:timestamp'`），在数据库中保留 7 天其重要身份信息（如姓名、身份证/护照号及证件图片）作为法律纠纷证据。
  * **Day 7+**：清理过期账户时，首先将 `leases` 表、中介评分表 `agent_ratings` 和房源评论表 `reviews` 中的 `tenant_id` / `user_id` 更新为 `null` 进行数据解耦（保留中介的历史租约与账单财务核查记录不受影响），然后物理删除 Storage 中的敏感证件图片，最后删除 `public.users` 的用户行。


---

### ✅ 5. AI 错误信息用户友好化

**完成时间：** 2026-06-02

**改动内容：**
- 503 错误：显示"AI 服务繁忙，请稍后再试"
- 网络错误：显示"无法连接到 AI 后端服务"
- 其他错误：显示"AI 助手暂时无法响应"

---

### ✅ 6. Ensuite 房型支持

**完成时间：** 2026-06-02

**改动内容：**
- 数据库：更新 room_type CHECK 约束，添加 'Ensuite'
- 中介端：ROOM_TYPES 数组添加 Ensuite
- 租客端：房源筛选添加 Ensuite 选项

---

### ✅ 7. 自动账单生成触发器

**完成时间：** 2026-06-02

**改动内容：**
- 创建 generate_lease_payments() 函数
- 创建 after_lease_insert 触发器
- 创建 033 号迁移文件

---

### ✅ 8. 收藏夹功能

**完成时间：** 2026-06-04

**改动内容：**
- 创建 favorites 表（user_id, unit_id）
- 创建 FavoritesManager 组件（爱心图标）
- 在房源卡片上添加收藏按钮
- 支持收藏/取消收藏

---

### ✅ 9. 租客评价系统

**完成时间：** 2026-06-04

**改动内容：**
- 创建 reviews 表（user_id, unit_id, rating, comment）
- 创建 ReviewSystem 组件（星级评分 + 文字评价）
- 评价资格限制：只有租约完成（到期/终止）的租客才能评价
- 在房源详情页添加评价区域

---

### ✅ 10. 可入住日期字段

**完成时间：** 2026-06-04

**改动内容：**
- units 表添加 available_from 字段
- 中介端房源表单添加日期选择器
- 租客端房源卡片显示"可入住: Jun 15"
- 租客端房源详情显示完整日期

---

### ✅ 11. 房源列表收藏筛选

**完成时间：** 2026-06-04

**改动内容：**
- 房源列表添加"我的收藏"筛选按钮
- 自动加载用户收藏列表
- 点击筛选只显示已收藏房源

---

### ✅ 12. 中介评分系统

**完成时间：** 2026-06-04

**改动内容：**
- 创建 agent_ratings 表（038号迁移）
- 租客可评价中介（仅限完成租约）
- 中介在个人主页查看评分汇总
- 每个租约只能评价一次

---

### ✅ 13. 商业文档

**完成时间：** 2026-06-04

**改动内容：**
- 创建 售卖策略/ 文件夹
- 包含：盈利模式、SaaS模式、系统功能、技术架构、部署指南、交接清单、定价策略、市场分析、竞品对比、产品规划与变现策略、小区知识库数据需求

---

### ✅ 12. 月收入趋势图改为折线图

**完成时间：** 2026-06-04

**改动内容：**
- Dashboard.tsx：将 BarChart 改为 LineChart
- 已收：实线绿色
- 应收：虚线蓝色

---

### ✅ 14. 收藏筛选刷新修复

**完成时间：** 2026-06-04

**改动内容：**
- 收藏后自动刷新收藏列表
- "我的收藏"筛选按钮实时更新

---

### ✅ 15. 中介评分显示

**完成时间：** 2026-06-04

**改动内容：**
- 创建 AgentRatingBadge 组件
- 房源卡片显示中介评分 ⭐4.5(3)
- 房源详情页显示中介评分
- 中介端个人主页显示评分汇总

---

### ✅ 16. 历史租约卡片字段顺序修复

**完成时间：** 2026-06-04

**改动内容：**
- 字段顺序调整：小区名+房型 → 价格 → Unit Number → 日期
- Unit Number 必须显示（中介和租客的交易记录）

---

### ✅ 17. RAG 知识库 + Embedding 模型升级

**完成时间：** 2026-06-05

**改动内容：**
- 创建 `rental_knowledge_base` 表，覆盖全马 42 所大学、132 个小区（92 个独立小区）
- 每个小区包含完整资料：价格范围、户型、评分（安全/卫生/性价比）、优缺点、交通、设施、距大学距离
- Embedding 模型从 `BAAI/bge-large-zh-v1.5`（1024 维）升级到 `BAAI/bge-m3`（1024 维，多语言支持更好）
- 修复维度不匹配问题：schema 从 VECTOR(1536) 统一改为 VECTOR(1024)
- 新增 `search_knowledge_base` Agent 工具，支持语义检索小区信息
- 新增 `import_knowledge_base.py` 导入脚本，一键灌数据 + 生成 embedding
- 创建 039、040 号 migration 文件

---

### ✅ 18. 外部房源搜索 + 搜索结果格式化

**完成时间：** 2026-06-05

**改动内容：**
- 放开 Tavily 外部平台搜索限制（原来硬编码排除 iProperty、PropertyGuru 等）
- 新增 `search_external_listings` 工具，专门搜索外部租房平台并返回结构化数据
- 搜索结果自动提取：价格（正则匹配 RM）、标题、摘要
- Agent 规则：搜索可执行，但不得向用户暴露来源链接/平台名称
- Agent 三引擎联动：知识库（小区资料）+ 外部房源搜索（实时在租）+ 通用搜索（政策/生活）

---

### ✅ 19. 前端知识库结果展示（MapAndCard 组件升级）

**完成时间：** 2026-06-05

**改动内容：**
- MapAndCard 组件新增知识库模式（`is_knowledge_base` prop）
- 知识库结果展示：小区名 + 大学名 + 房产类型
- 价格范围标签（绿色 RM min-max/月）
- 评分标签（金色星星 overall、蓝色盾牌 safety）
- 描述文字（两行截断）
- 地图标注 + 通勤路线计算

---

### ✅ 20. RAG 知识库 + Embedding 向量化（2026-06-05）

**改动内容：**
- 新建 `rental_knowledge_base` 表，42 所大学、132 个小区
- 使用 `BAAI/bge-m3` 模型（SiliconFlow 免费 API）生成 1024 维向量
- 导入脚本 `import_knowledge_base.py` 一键灌数据 + 生成 embedding
- Agent 新增 `search_knowledge_base` 工具，语义搜索小区信息
- 知识库 + 联网搜索双引擎回答

---

### ✅ 21. Manus AI 风格聊天界面重设计（2026-06-05）

**改动内容：**
- AIChat.tsx 完全重写，单页面全过程可视化
- 思考步 → 工具卡实时更新 → 文字结论 → 地图/卡片
- 工具卡支持展开查看原始 JSON 输出
- 欢迎页 6 个双语快捷提示卡片
- 聊天记录 localStorage 存储 + 历史记录面板
- 设计系统集成（Bento Grid、柔和阴影、teal 配色）

---

### ✅ 22. 外部搜索解锁 + 评价系统修复（2026-06-05）

**改动内容：**
- 移除 Tavily 外部平台搜索限制（可搜不可说来源）
- 管理员可删任何评价（RLS 策略）
- 每人每房只能评一次（唯一约束）
- 评价显示用户名而非 UUID
- AI 错误信息中文化
- Gemini 模型从 2.5-flash（20次/天）切到 2.0-flash（1500次/天）

---

### ✅ 23. 多页面路由架构重构 + 未登录浏览房源

**完成时间：** 2026-06-05

**改动内容：**
- 将单页面标签切换重构为 Next.js App Router 多页面路由
- 每个功能有独立 URL（/listings、/chat、/my-lease、/admin/* 等）
- 浏览器地址栏随页面变化，后退/前进按钮正常工作
- 提取 AuthContext 和 PendingCountsContext 实现跨路由状态共享
- 提取 AppSidebar 和 AppTopbar 为独立组件
- `/listings` 页面公开可访问，未登录用户可浏览房源（只读模式 + 登录引导）
- 页面顶部蓝色横幅引导未登录用户登录
- page.tsx 从 643 行巨石变为 3 行 redirect
- 新建 19 个文件，修改 3 个文件

---

### ✅ 24. 游客模式 Guest 页面

**完成时间：** 2026-06-05

**改动内容：**
- 独立 `/guest` 路由，极简顶栏（无侧边栏）
- 圆形 Logo 徽章 + 渐变色艺术字品牌名
- 10 种语言"你好"浮动文字动画
- Hero 从底部滑入 + Logo 入场动画
- 3×2 特性/流程网格
- 信任保障区块（对比其他平台假房源问题）
- Testimonials 区块（pravatar.cc 真人头像）
- 分页（每页 8 个）
- 底部导航（服务条款弹窗、隐私政策弹窗、费用计算器、申请中介）
- 语言切换按钮（无暗黑模式）
- Middleware 智能重定向

---

### ✅ 25. 房源列表分页

**完成时间：** 2026-06-05

**改动内容：**
- 所有用户（游客/租客/中介）支持分页
- 网格视图：游客 4 列×2 行=8 个，登录用户 5 列×2 行=10 个
- 列表视图：每页 8 行
- 分页控件：上一页 / 页码按钮 / 下一页
- 筛选变化自动回到第 1 页

---

### ✅ 26. 动态页面标题

**完成时间：** 2026-06-05

**改动内容：**
- 浏览器标签页标题随页面变化
- 主页面按 tab 映射中英文标题
- 每个独立路由页面设置自己的 document.title
- 标题跟随语言切换

---

### ✅ 27. 入住人数计算修复

**完成时间：** 2026-06-05

**改动内容：**
- 修复中介确认意向后入住人数立即增加的 Bug
- 入住人数改为基于实际活跃租约数量
- 确认意向不再影响入住人数，只有创建租约才增加

---

### ✅ 28. 身份验证进度条

**完成时间：** 2026-06-05

**改动内容：**
- 登录用户访问 /listings 时显示顶部进度条
- 进度条从 0% 平滑填充到 100%
- 校验完成后自动重定向到对应页面

---

### ✅ 29. 登录认证回调修复（Magic Link + OAuth）

**完成时间：** 2026-06-06

**改动内容：**
- `auth/callback/route.ts` 同时处理 `code`（OAuth/PKCE）和 `token_hash` + `type`（邮箱 Magic Link）
- Supabase 客户端 cookie 操作绑定到 `NextResponse.redirect()`，确保 session cookie 在重定向前写入
- 增加 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 回退，避免 key 配置不一致

**已知风险：** 首次 Magic Link 登录若 cookie 尚未就绪，`AuthContext` 可能短暂读到 `role=null`，`/listings` 会误判未登录并跳转 `/guest`（需第二次登录）。若复现，优先检查 callback cookie 写入与 `listings/page.tsx` 的 auth 等待逻辑。

---

### ✅ 30. Guest / Login 页面 UI 美化

**完成时间：** 2026-06-06

**改动内容：**
- **Guest 页**：CSS 国旗字 `flag-wordmark`（红白条纹 + 蓝区渐变 + 新月/十四角星 SVG data URI）、响应式 `.guest-grid` / `.guest-grid-4`、滚动 reveal 动画、信任 pill、底部 CTA
- **Guest 滚动动画可逆化**：`.reveal` 进入视口淡入上滑，离开视口反向淡出；`.from-above` 区分从上方/下方离开的方向
- **Login 页**：桌面分屏（品牌面板 + 登录卡）、极光渐变背景、柔和 blob、多语言「你好」漂浮、院校条、信任 pill；Logo 置于 wordmark 正上方

---

### ✅ 31. 登录后 UI 美化（侧边栏 + 二级 Tab + 租客端）

**完成时间：** 2026-06-06

**改动内容：**
- **一级导航（侧边栏）**：激活项渐变背景 + 左侧发光条 + 悬停微移/图标放大
- **二级 Tab（`.seg-tabs`）**：激活态品牌渐变 + 投影；未激活悬停反馈
- **中介「新增房间」表单**：`maxWidth: 720`；小区与房型同一行；可入住日期限宽；`.form-row` 手机端单列
- **租客端**：统一 `.empty-state`（无租约引导 +「去找房源」CTA）；租约 stat-chip 药丸；个人资料 `.grid-2` 响应式；消息中心/房源筛选渐变 pill；房源数量胶囊徽标

---

### ✅ 32. 单元号 `#` 前缀 Bug 修复

**完成时间：** 2026-06-06

**改动内容：**
- `AdminPanel.formatLeasePropertyLabel()` 不再在 `unit_number` 前加 `#`
- 收租核查表归档区、付款审核弹窗、租客端历史租约等全局统一为 `A-12-3` 格式（马来西亚惯例：栋-楼-号，非美式 `#` 前缀）
- 涉及：`AdminPanel.tsx`、`TenantPortal.tsx`

---

### ✅ 33. Google OAuth 登录后误跳 Guest 页修复

**完成时间：** 2026-06-06

**问题：** Google 登录 `redirectTo` 使用 `next=/`，中间件将 `/` 无条件重定向到 `/guest`，导致已登录用户落在 Guest 页（无侧边栏）。

**修复：** `login/page.tsx` 中 Google OAuth 与 Magic Link 统一为 `next=/listings`。

**跳转链（修复后）：** 登录 → `/auth/callback` → `/listings` → 租客看列表 / 中介自动 `/admin/dashboard`。

---

### ✅ 34. 所有上传照片/视频客户端强制压缩

**完成时间：** 2026-06-06

**改动内容：**
- **证件/合同/凭证压缩**：前端对用户上传的护照、身份证、学生卡、工作证明、支付凭证及房源照片强制进行 JPEG Canvas 压缩（分辨率和质量预设），体积平均缩减 80% 以上。
- **看房视频压缩**：开发 `compressVideo.ts` 对看房视频（WebM）在浏览器端重编码，将分辨率限制在 ≤1280x720 且码率限制在 1.2 Mbps，大幅节省 Storage 存储空间。
- **头像文件压缩**：限制中介头像在 Canvas 端压缩至 30KB 以下。

---

### ✅ 35. 租客注销 7 天敏感数据留存与自动清理（收租历史解耦保留）

**完成时间：** 2026-06-06

**改动内容：**
- **双语退订告示**：在 `AppTopbar.tsx` 账户注销警告弹窗中，新增中英双语公告，告知租客注销后个人敏感信息（如证件照片）将安全留存 7 天供取证防损防犯罪使用，并在 7 天后自动销毁。
- **定时清理任务 (Server Action)**：更新 `deleteAccount.ts`，自动轮询清理已注销超 7 天的账户。
- **中介账单/评分解耦 (Decoupling)**：彻底删除前自动将租约、中介评分及房源评论的外键设为 `null`，确保中介后台所有的归档租约和收租流水（payment_records）不被物理删除，历史数据完整无缺。
- **MockParity 同步**：在前端 `AuthContext.tsx` 中同步为 LocalStorage Mock 数据库实现了一套完全相同的 7 天过期清理与租约/评分的 `null` 解耦机制。

---

### ✅ 36. 租客/中介门户绝对隔离

**完成时间：** 2026-06-06

**改动内容：**
- 租客与中介拆分为独立注册/登录入口（`/register/tenant`、`/register/agent`）
- 角色写入 `user_metadata.role`（`student` | `agent`），`AuthContext` 与 `middleware.ts` 按角色做路由隔离
- 新建 `agent_profiles`、`email_verifications` 表；扩展 `users` 表证件字段
- 租客支持邮箱+密码或 Google 登录（首次需在 `/profile` 补身份资料；`/register/complete-profile` 保留兼容）
- 中介仅支持邮箱+密码（审批通过后登录），不再提供 Google 按钮
- 移除旧 Magic Link 登录入口

**遗留兼容（已手动处理，见下方「已知问题」）：**
- 老用户 `user_metadata.role` 需 SQL 批量回填
- 超管账号需补 `agent_profiles` 记录 + 设置密码后才能走中介登录

---

### ✅ 37. Google OAuth 登录死循环修复

**完成时间：** 2026-06-06

**问题：** 谷歌登录成功后，若 `user_metadata.role` 为空，回调重定向补资料页时新建了**不带 auth cookie** 的响应，导致 session 丢失 → 检测无用户 → 跳回 `/login` → 无限循环。

**修复：** `auth/callback/route.ts` 在重定向时，将已写入 session 的 cookie 复制到新 redirect 响应上。补资料目标现为 `/profile`（#42）。

**跳转链（修复后）：** Google 授权 → `/auth/callback`（cookie 保留）→ `/profile`（补身份资料）→ `/listings`。

> 注：2026-06-06 后续已将默认补资料页从 `complete-profile` 统一到 `/profile`（见 #42）。

---

### ✅ 38. 浏览器记住密码 / 自动填充

**完成时间：** 2026-06-06

**问题：** 登录页和注册页的 `<input>` 缺少 `name` 和 `autocomplete` 属性，浏览器无法识别账号密码字段，导致既不弹出「保存密码」，也不会在下次登录时自动填充。这是纯前端的 HTML 语义问题，无需任何服务器端改动。

**修复：**
- **登录页** `login/page.tsx`（租客 + 中介两套表单）：邮箱框加 `name="email"` + `autoComplete="username"`；密码框加 `name="password"` + `autoComplete="current-password"`。浏览器据此弹出保存提示并在下次自动补全。
- **注册页** `register/tenant/page.tsx`、`register/agent/page.tsx`：邮箱框加 `autoComplete="username"`；设置密码框加 `autoComplete="new-password"`（注册场景，浏览器会提示生成/保存新密码，且不会用旧密码乱填）；确认密码框同样用 `new-password`。
- `register/complete-profile/page.tsx` 无密码框，无需改动。

**预期效果：** 用户登录/注册后浏览器自动提示保存账号密码，下次登录可一键补全，相当于免费实现「记住密码」，全部依赖浏览器原生密码管理器。

---

### ✅ 39. 删除房源后列表不即时刷新

**完成时间：** 2026-06-06

**问题：** 中介删除房源后 Toast 提示成功，但「已登记房源库」表格仍显示该房源，需手动刷新页面才消失。

**根因：** `loadAll()` 在 `isLoaded === true` 时直接 `return`，删房源后调用的 `loadAll()` 成为空操作；UI 仍显示旧的 `units` 状态。

**修复：**
- `loadAll(force)` 增加 `force` 参数，`force=true` 时跳过早退守卫
- `deleteUnit` 成功后立即 `setUnits` / `setLeases` / `setInterests` 乐观过滤本地状态
- 增删改操作统一改为 `loadAll(true)` 强制与 Supabase 同步
- 同步调用 `refreshListingsCache({ force: true })` 更新租客端列表缓存

**涉及文件：** `AdminPanel.tsx`

---

### ✅ 40. Tab 切换性能优化（房源列表缓存 + 中介单实例）

**完成时间：** 2026-06-06

**问题：** 一级导航 tab 切换时有明显卡顿；房源列表每次进入都转圈重载。

**已实施方案：**

| 优化项 | 实现 | 文件 |
|--------|------|------|
| 房源列表 Context 缓存 | `ListingsDataContext`：首次加载后内存保留；切回 tab **先显示缓存**，后台静默刷新；点「刷新」才 `force` 全量拉取 | `ListingsDataContext.tsx`, `PropertyListings.tsx`, `(app)/layout.tsx` |
| 中介 AdminPanel 单实例 | `AdminShell` 挂 `admin/layout.tsx`，`pathname` → `activeTab`；dashboard/properties/leases 等切换时 **不重挂载** 5,800 行组件 | `AdminShell.tsx`, `admin/layout.tsx`, `admin/*/page.tsx` |
| 房源子视图 `display:none` | 库存表、新增表单保留 DOM 用 `display:none` 隐藏（切换子 tab **不丢未保存表单**；按需挂载已回退） | `AdminPanel.tsx` |
| `loadAll(force)` | 首次 `isLoaded` 挡重复；增删改后 `loadAll(true)` | `AdminPanel.tsx` |

**资源影响：** 主要占用浏览器内存；同会话 Supabase 读次数**减少**；Vercel / Render 无影响。详见 `docs/performance-diagnosis.md`。

**残留（按体感再优化）：** 租客 `TenantPortal` 路由仍各自挂载；切 `/admin/listings` 或 `/admin/inbox` 时 `AdminPanel` 仍会卸载（但房源浏览已受益于列表缓存）。

---

### ✅ 41. 内部 tab 冗余请求清理 + ReviewSystem 批量查询

**完成时间：** 2026-06-06

**改动内容：**
- **P0-2 收尾：** 移除 `AdminPanel` 内部 tab 栏 onClick 中的 `loadAll(true)`、`fetchFeedbacks()`、`fetchAllReviews()`；首次进入各 tab 由 `resolvedTab` effect 按需加载；增删改后仍 `loadAll(true)` 保新鲜度
- **P2-3 ReviewSystem：** 评价列表由「每条 review 单独查 users」改为一次 `users.in('id', userIds)` 批量取姓名（20 条评价：21 次请求 → 2 次）

**涉及文件：** `AdminPanel.tsx`, `ReviewSystem.tsx`

---

### ✅ 42. 租客身份验证三层统一

**完成时间：** 2026-06-06

**背景：** 早期 `023` 个人信息页证件为选填；门户隔离后注册页有强制证件，但老租客可绕过。登录后补资料入口从 `complete-profile` 统一到 `/profile`；**进入租客端后**新/老租客共用同一 `/profile`（新注册仍走 `/register/tenant`，UI 与 `/profile` 不同）。

**已实施：**

| 层级 | 行为 |
|------|------|
| 登录门禁（硬） | 无 `role` 或无 `identity_type` → 只能访问 `/profile` |
| 保存资料（硬） | `/profile` 必选身份 + 按类型必传证件（IC 正反面或护照照片页） |
| 表达意向（软） | 资料不齐全 → Modal 列出缺失项；可「去完善」或「仍要提交」 |

**涉及文件：** `TenantPortal.tsx`, `tenantIdentityUtils.ts`, `TenantIdentityGate.tsx`, `TenantIdentityWarningModal.tsx`, `middleware.ts`, `login/page.tsx`, `auth/callback/route.ts`, `PropertyListings.tsx`, `(app)/layout.tsx`

**登录后补资料重定向：** 密码登录、OAuth 回调、中间件在缺 `role`/`identity_type` 时 → `/profile`（非新租客注册；注册仍用 `/register/tenant`）。`complete-profile` 保留兼容，非默认入口。

**FAQ：** 见 `docs/FAQ.md`「租客身份验证有哪些要求？」

---

### ✅ 43. 管理员房源浏览独立组件（AdminListingsBrowse）

**完成时间：** 2026-06-06

**问题：** `/admin/listings` 复用 `PropertyListings readOnly`，仍加载租客意向/收藏/合租等逻辑，仅 UI 隐藏。

**改动：**
- 新建 `AdminListingsBrowse.tsx` — 轻量浏览（筛选、网格/列表、详情预览）
- `AdminShell` 改用新组件；`PropertyListings` 移除全部 `readOnly` 分支
- `lib/listingDisplayUtils.ts` — 共享图片/中介标签工具

---

### ✅ 44. 新增房源面积 (Area) 字段及全端同步显示

**完成时间：** 2026-06-08

**问题：** 中介在管理房源时缺少房源面积指标，租客和中介在浏览时无法直观了解单位的实际大小。

**改动内容：**
- **数据库架构**：新增 `047_add_unit_area.sql` 迁移，往 `units` 表添加可选 `area` (INTEGER) 列，并同步更新了 `supabase/schema.sql` 和 `newSQL.sql`。
- **中介表单设计**：在中介添加/编辑房源的表单中新增了面积字段，与可入住日期字段并排进行响应式双列展示；完善了新建、编辑、重置和复制房源（Copy listing）状态下的面积逻辑。
- **租客与游客界面**：在房源网格卡片、房源详细列表行、房源详情弹窗（Drawer）中同步添加了面积的展示，并统一使用 `lucide-react` 中的 `Maximize` 尺寸图标。
- **缴租台账集成**：在租客端的活跃租约缴租台账卡片（`LeaseLedgerCard`）头部，也同步渲染了该房源的面积。

---

### ✅ 45. AI 聊天 Tab 切换后台持久化与不卡顿优化

**完成时间：** 2026-06-08

**问题：** 租客在 AI 助手交互生成答案时，切换至其他功能页面（Tab）会导致正在进行的生成连接及会话进程彻底丢失。

**改动内容：**
- **全局状态解耦**：设计并实现了全局的全局单例管理器 `GlobalChatState`，将 active 流、生成状态和 AbortController 脱离 React 组件周期单独维护。
- **不卡顿后台写入**：组件卸载后，聊天流仍在后台稳定执行并实时存盘至 `localStorage`。由于卸载后无 React 界面重绘与 DOM 消耗，完美避开了用户卡顿风险。
- **进程重连与同步**：当用户切回 AI 助手页面时，组件重新挂载并从全局单例重连流订阅，实现无缝衔接继续生成并输出打字机动效。

---

### ✅ 46. 租客端 Tab 切换与加载双重 Loading 性能优化

**完成时间：** 2026-06-08

**问题：** 租客在初次登录进入租约页面或在路由 Tab 间切换时，会出现“转圈 Spinner”与“骨架图 Skeleton”的双重等待，视觉上产生了卡顿与延迟。

**改动内容：**
- **门禁缓存穿透优化**：修改 `TenantIdentityGate.tsx`，使其读取并更新全局 context 中的 `profileIdentityType` 缓存。在首次校验通过后，切换其他页面即可免除重复数据库网络请求与 Spinner 渲染，实现零延迟无感放行。
- **工单加载并发与 Join 重构**：将 `loadMyFeedbacks` 内部的 4 次串行网络/服务器请求精简为 1 次 select join 查询，并使用 `Promise.all` 将其与主租约 `load`、个人资料 `loadProfile` 并行拉取，消除了原先的瀑布流式等待，首屏加载效率大幅提升（减少约 800ms）。

---

### ✅ 47. 同小区房源展示改版与中介端详情页同步

**完成时间：** 2026-06-08

**问题：** 详情弹窗中的同小区其他房间展示过于冗长（占用大量垂直空间），且跳转链接不够精致。同时，中介端浏览房源时缺少同小区关联房源展示以及租客评论区（及骨架图占位）。

**改动内容：**
- **精简小方块排版**：将原本的整行纵向列表改造成紧凑的并排小方块（最多展示 5 个），包含缩略图、房型与租金，支持 HSL 语义化悬浮放大与立体阴影动效，且不产生横向滚动条。
- **“查看更多”弹出框**：在同小区房源标题旁增加“查看更多”超链接文本，带 hover 下划线微动效。点击后弹出毛玻璃 Modal 展示同小区的所有房源（包含当前房源并用“当前”标签高亮）。
- **中介端详情页同步**：向 `AdminListingsBrowse.tsx` 中置入同小区小方块布局、查看更多弹出框，以及租客评论系统（`<ReviewSystem />`）。
- **评论骨架屏占位**：统一为中介端和租客端切换不同房源时，评论区加载未完成时渲染平滑渐变的 Skeleton 骨架屏占位，避免页面发生抖动。
- **全端同步与角色隔离原则**：房源列表及详情页的任何功能或视觉重构均遵循“全端同步”规范，以确保两端产品视觉与数据逻辑的高度统一。在此基础上，通过条件控制进行角色功能隔离（例如中介端剔除“我要租”、“联系中介”等与中介角色冲突的按钮和状态显示，只保留只读信息与必要的后台管理交互）。

---

### ✅ 48. 个人信息页面排版重构与 Onboarding 引导

**完成时间：** 2026-06-08

**问题：** 租客个人信息页面表单横向拉伸过长，填表体验欠佳；身份选择大卡片过长、Emojis 在 Windows 系统显示欠佳；进度条 100% 完成度时颜色为警告橙黄色而非成功绿色；新租客注册登录后缺少友好的迎新 Onboarding 与身份验证提示引导，引导力度不足。

**改动内容：**
- **表单最大宽度限制与居中**：将个人信息外层玻璃卡片限制最大宽度为 `800px`，使用 `margin: 0 auto` 居中对齐，保证宽屏显示器上的阅读与操作流畅性。
- **三列响应式身份卡片网格**：将原横跨整屏的垂直列表身份卡片重构为 3 列并排网格，每个身份类型均搭载精美专业的 SVG 矢量图标（`CreditCard`、`GraduationCap`、`Globe`），在不同分辨率下完美自适应折行。
- **进度条翡翠绿渐变**：当个人资料完善度达到 100% 时，进度条终点色彩从警告色平滑过渡为健康的翡翠绿（`var(--success)`），提升用户获得感。
- **迎新 Onboarding 引导横幅**：针对未验证的新注册租客，在页面头部渲染一个毛玻璃渐变色的 `👋 欢迎来到 Malaysia Ez Rent！` Onboarding 横幅，突出强调强制要求“选择身份类型并上传对应证件才能使用房源浏览、租约等功能”的警示要求，指引明确且富有高级感。
- **个人信息骨架屏同步重构**：重构 `TenantSkeleton` 的 `mode === 'profile'` 骨架，改用限制最大宽度 `800px` 且居中对齐的卡片容器，并精确放置标题、迎新横幅、进度条、四宫格输入项及三列身份选项卡的 Shimmer 块，彻底消除因骨架屏与真实表单布局结构 and 宽度差异导致的加载过渡跳跃与布局抖动。

---

### ✅ 49. 忘记密码 / 重置密码（Magic Link 老租客兼容）

**完成时间**：2026-06-09

**改动内容**：
- **登录页入口**：在租客和中介登录表单上均添加了「忘记密码？」链接，并实现了一个磨砂玻璃重置弹窗 UI，用于提交用户的注册邮箱。
- **重置邮件流程**：调用 `supabase.auth.resetPasswordForEmail()` 将重置链接发送至用户邮箱。
- **独立重置页面**：新建了 `/reset-password` 路由页面，在校验密码长度、强度的同时，调用 `supabase.auth.updateUser()` 设置新密码，并在完成后自动根据角色（租客或中介）进行路由分流跳转。
- **中间件与回调安全放行**：修改 `middleware.ts` 与 `auth/callback/route.ts` 放行重置密码路径，避免未登录/未完善资料的用户触发强制拦截与死循环。
- **Mock 仿真流支持**：提供本地 Mock 模式下的仿真密码重置链路，一键生成重设凭证链接。

---

### ✅ 50. 地图卡片定位缓存、USM检索质量与主数据库同步修复

**完成时间**：2026-06-09

**改动内容**：
- **地图 Iframe 缓存清除**：在 `MapAndCard.tsx` 的地图 `<iframe>` 上配置 `key={mapUrl}`，利用 React key 的差异化强行重挂载组件，绕过浏览器缓存，使卡片切换时地图能瞬时重绘并更新至正确定位。
- **关联大学信息嵌入**：优化向量计算逻辑，将 `university_name` 并入 Embedding 生成文本。重新运行 `import_master_database.py` 对全库 153 个小区重算向量向量，大幅提升 USM（理科大学）搜索相似度至 `0.6` 以上（超过匹配门槛 `0.5`）。
- **通勤解析名称校正**：在 `agent.py` 的 `calculate_commute` 回包合并中，双向校验结构化地理路名与原始 LLM 参数，确保地理编码转换后小区资料能顺利拼贴至地图组件上。

---

## 待完成功能


---

## 已知问题与潜在风险（2026-06-06）

| 问题 | 原因 | 现状 / 建议 |
|------|------|-------------|
| Magic Link / Google 首次登录回到 `/login?error=auth_failed` 或需登两次 | PKCE cookie 时序、callback 只处理 `code` 未处理 `token_hash`、cookie 未写入 redirect 响应、**且 `AuthContext` 只在挂载时 `getUser()` 检查一次、role=null 即被 `/listings` 踢回 `/guest`** | callback 已修复双路径 + cookie 绑定；**`AuthContext` 已改为订阅 `onAuthStateChange`（`INITIAL_SESSION`/`SIGNED_IN`），session 注水前不会锁定 role=null，竞态已消除**；若仍复现，检查 Supabase Site URL、邮件链接域名与 Vercel 环境一致 |
| Google 登录后无限回到 `/login` | 门户隔离后 `role` 为空时 callback 重定向补资料页但未携带 auth cookie | **已修复**（#37）：重定向时复制 session cookie |
| 超管/老中介无法用 Google 登录中介端 | 新系统中介入口移除 Google；`role` 依赖 `user_metadata`；登录流程要求 `agent_profiles` 记录 | **已手动处理**：SQL 补 `role=agent`、设密码、插入 `agent_profiles`；长期建议 AdminPanel 增加「兼容旧超管」或统一迁移脚本 |
| 老租客 `user_metadata.role` 为空被 middleware 拦截 | 门户隔离迁移未自动回填 role | **已手动处理**：SQL 批量补 `role=student`（非 admin_users 邮箱） |
| 老租客有 `role` 但无 `identity_type` 可绕过补资料 | 中间件只查 `role`、个人信息页证件曾为选填 | **已修复**（#42）：统一门禁 → `/profile`；保存时强制证件 |
| Magic Link 老租客无法登录（无密码、无 Google） | 登录页移除 Magic Link，仅保留密码 + Google | **待实施**：见「待完成功能 #1 忘记密码/重置密码」；临时可在 Supabase 后台为单个用户手动设密码 |
| 登录成功却落在 `/guest`（无侧边栏） | Google OAuth 曾用 `next=/` → 中间件 `/` → `/guest` | **已修复**：OAuth 改为 `next=/listings` |
| `/listings` 闪一下再跳 Guest | `AuthContext` 加载中 `role=null`，`listings/page.tsx` 误判未登录 `router.replace('/guest')` | **已修复**：`AuthContext` 在 `onAuthStateChange` 解析出 session 前保持 `loading=true`，`/listings` 期间显示进度条而非误跳；未登录时 `return null` 避免闪烁 |
| 根路径 `/` 永远进 Guest | `middleware.ts` 无条件 `pathname === '/'` → `/guest` | 设计如此（公开入口）；**勿**把 OAuth/Magic Link 的 `next` 设为 `/` |
| 删除房源后列表不更新 | `loadAll()` 在 `isLoaded` 后为空操作 | **已修复**（#39）：乐观更新 + `loadAll(true)` |
| Tab 切换卡顿 / 房源列表每次转圈 | 路由重挂载 + 无内存缓存 | **已修复**（#40–#41）：`ListingsDataContext` + `AdminShell`；详见 `performance-diagnosis.md` |
| 浏览器不提示保存密码 | 登录/注册 input 缺 `name` / `autocomplete` | **已修复**（#38） |
| 美化改动影响业务逻辑 | 纯 CSS/布局 | 新增行为：无租约空状态「去找房源」按钮（`router.push('/listings')`）；其余为显示层 |

---

## 1. 房源列表性能优化

**当前状态：** PropertyListings 组件一次性加载全部房源数据到内存，所有卡片同时渲染到 DOM，没有任何分页、虚拟滚动或图片懒加载。

**触发条件：** 房源数量超过 100 套时开始需要关注，超过 200 套会明显卡顿。

### 第一阶段：图片懒加载 + 分页（推荐优先实施）

**触发条件：** 100+ 套房源

**改动内容：**
- 给所有房源卡片的 `<img>` 标签加上 `loading="lazy"` 属性，让浏览器自动延迟加载视口外的图片
- 添加分页组件（每页 24 套），用户点击页码切换，避免一次性渲染大量 DOM 节点
- 改动范围小，只需修改 `PropertyListings.tsx` 中的网格渲染部分

**预期效果：** 初始加载只渲染 24 张卡片（~720 个 DOM 节点），图片按需加载，页面流畅度大幅提升。

### 第二阶段：Supabase 服务端分页 + 搜索下推

**触发条件：** 500+ 套房源

**改动内容：**
- 将 `supabase.from('units').select('*')` 改为 `supabase.from('units').select('*', { count: 'exact' }).range(offset, offset + limit)`
- 将搜索筛选从前端 `.filter()` 下推到数据库层（`.ilike('community.name', '%keyword%')`）
- 分页状态（页码、每页数量）反映到 URL query params，支持直接链接分享

**预期效果：** 每次只传输 24 条数据，网络传输量降低 95%+，数据库查询效率也更高。

### 第三阶段：虚拟滚动

**触发条件：** 1000+ 套房源，或需要无限滚动体验

**改动内容：**
- 安装 `react-virtuoso`（或 `@tanstack/react-virtual`）
- 将网格/列表替换为虚拟滚动容器，只渲染视口内可见的卡片
- DOM 节点数恒定在 ~50 个左右，不随数据量增长

**预期效果：** 即使 10000 套房源，页面也保持流畅，内存占用恒定。

---

## 2. 实时更新（Supabase Realtime）

**当前状态：** 房源数据（`units`、`communities` 表）没有 Supabase Realtime 订阅。用户需要手动刷新才能看到新房源。

**触发条件：** 多个中介同时管理房源，或者租客需要及时看到新房源时。

**改动内容：**
- 在 `PropertyListings.tsx` 中添加 Supabase channel 订阅：
  ```typescript
  supabase.channel('public:units')
    .on('postgres_changes', { event: '*', schema: 'public', table: 'units' }, () => {
      loadListings(); // 重新加载房源列表
    })
    .subscribe();
  ```
- 订阅 `communities` 表的变更
- 组件卸载时清理 channel

**预期效果：** 新房源上线、状态变更时，所有在线用户的浏览页面自动更新，无需手动刷新。

---

## 3. 新房源通知功能

**当前状态：** 租客无法主动感知新房源上线，需要自己反复查看。

**触发条件：** 租客设置筛选条件后希望被动接收匹配房源。

**改动内容：**
- 新增 `listing_alerts` 表，存储用户的筛选条件（小区、房型、最高租金等）
- 当新房源插入 `units` 表时，通过 Supabase Database Function 触发通知匹配逻辑
- 通知方式：站内通知（Inbox 组件）+ 可选邮件通知
- 在租客端添加"设置房源提醒"入口

**预期效果：** 租客设定条件后，符合条件的新房源上线时自动收到通知，提升转化率。

---

## 4. ~~Dashboard 社区分布图优化~~ ✅ 已完成

**已完成：** 社区分布图标题右侧添加"查看全部 →"链接，点击跳转到房源管理 → 已登记房源库。

**未来可考虑：** 点击柱状图的某个小区直接筛选显示该小区的房源列表。

---

## 5. 地图多标记点聚合

**当前状态：** MapAndCard 组件只显示单个房源的地图标记（Google Maps Embed iframe），不支持多标记点。

**触发条件：** 需要在地图上同时展示所有房源位置时。

**改动内容：**
- 从 Google Maps Embed API 迁移到 Google Maps JavaScript API（或 Mapbox）
- 使用 `@googlemaps/markerclusterer` 实现标记点聚合
- 点击聚合标记展开查看具体房源
- 地图与列表双向联动（点击地图标记高亮列表卡片，反之亦然）

**预期效果：** 用户可以在地图上直观看到所有房源分布，密集区域自动聚合，点击展开查看详情。

---

## 6. 中介订阅系统

**当前状态：** 所有中介免费使用全部功能。

**触发条件：** 用户量达到 50+ 中介时开始考虑变现。

**改动内容：**
- 定义套餐（免费版/专业版/企业版）
- 集成 Stripe 支付
- 根据套餐限制房源数量
- 添加订阅管理页面

**预期效果：** 实现商业化变现，月收入 RM 10,000+。

---

## 7. 推广服务

**当前状态：** 所有房源平等展示。

**触发条件：** 中介有推广需求时。

**改动内容：**
- 定义推广套餐（置顶/首页推荐/精准推送）
- 修改排序算法，付费房源优先展示
- 添加推广管理后台

**预期效果：** 增加收入来源，月收入 RM 5,000+。

---

## ~~8. 小区知识库~~ ✅ 已完成（2026-06-05）

**实际实现：**
- 新建 `rental_knowledge_base` 表（非 community_knowledge）
- 录入 132 个小区（42 所大学，92 个独立小区）
- 使用 `BAAI/bge-m3` 生成 1024 维 embedding
- Agent 新增 `search_knowledge_base` 工具
- 知识库 + 联网搜索双引擎回答

---

## 9. 网银 FPX 扫码收租 (自动对账)

**当前状态**：租客缴租时使用手机拍照并手动上传转账截图，中介需在管理端进行人工对比审核。

**触发条件**：中介管理的房源量大，希望实现“无感对账”。

**改动内容**：
- 集成马来西亚本地网银 FPX 支付通道（如通过 Stripe 结算）。
- 租客直接拉起网银付款，支付成功后系统通过 Webhook 自动标记账单状态为已付，账目自动归档。

**预期效果**：收租效率提升 95%，免去中介人工看图对账麻烦。

---

## 10. 自动化 WhatsApp / 短信账单提醒

**当前状态**：每月账单自动生成，但中介需手动在群聊里发送链接提醒。

**触发条件**：中介有自动催租需求。

**改动内容**：
- 接入 WhatsApp Business API / 短信网关服务。
- 账单生成后自动发送模板提醒给租客，带专属付款短链。

**预期效果**：降低逾期率，节省中介群发消息时间。

---

## 11. AI 租客线索智能匹配 (AI Lead Matching)

**当前状态**：AI 助手只负责帮租客查找匹配房源，无房源时无后续跟进。

**触发条件**：中介希望平台能直接为其输送精准客流线索。

**改动内容**：
- 当租客表达找房诉求但平台暂无匹配房源时，AI 自动生成“找房意向线索”。
- 系统基于地理位置和预算，自动匹配并通知在该小区有挂牌记录的专业中介，打通“AI 精准分发线索”。

**预期效果**：房源与租客意向飞轮式匹配，提升中介开单率。

---

## 12. 中介 AI 房源推广文案自动生成 (AI Co-pilot)

**当前状态**：中介录入房源需要手动撰写描述，文案吸引度不一。

**触发条件**：中介希望快速生成社交网络（小红书/Facebook 等）引流文案。

**改动内容**：
- 房源录入表单上增加“AI 生成文案”按钮。
- AI 自动基于房型、价格、小区亮点，生成格式化的小红书种草风、Facebook 招租风的中英文推广文案，并提供社交媒体海报排版。

**预期效果**：降低中介录入编写成本，统一房源介绍美感。

---

## 13. 付费增值：AI 租房合同解读助手 (基于 API 计费)

**当前状态**：国际生面对全英文长篇 Tenancy Agreement 合同非常头疼，存在条款纠纷隐患。

**触发条件**：国际生收到中介发送的英文合同，需要快速了解关键条款规避风险。

**方案（按次/按量 API 计费）**：
- 新建“AI 合同助手”工具。
- 允许用户上传 PDF 格式的租房合同，AI 自动提取关键条款（如转租规则、押金退还细则、违约金计算等）并用其中文或英文母语进行总结解释。
- **商业化**：由于此功能需要调用第三方 LLM（如 Gemini/GPT）接口并进行大量 Token 计算，平台将针对此项服务单独收费（如每次读取收费 RM 10 - 20），以冲抵 API 成本并获取溢价利润。

